namespace Agent.Abstract.Models
{
    public enum AgentType
    {
        Ui,
        DbManager,
        FileManager,
        ConnectionAnalyzer,
        Splitter,
        Worker,
        Analyser
    }
}